# Staging environment reference — sanitized template
# Real values pulled from vault at deploy time

LOG_LEVEL=debug
INGEST_ENDPOINT=https://logs-staging.internal.example.com/v1/ingest
RETRY_COUNT=3

# TEMP: vault migration incomplete, ticket MISSION-1 tracks this
# Do not merge to main until MISSION-1 is resolved
VAULT_REF=vault://staging/mission-archive/creds
